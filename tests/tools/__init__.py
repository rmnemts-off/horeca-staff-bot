"""Инструменты, которые в поставку не входят (решение D13, план — задача 44).

Каталог живёт под ``tests/`` намеренно. ТЗ 12 разрешает данным существовать только внутри
``tests/``, а раздел 10.1 разрешает залить фрагмент референсной книги во временное
окружение и потом удалить. Всё, что читает референсную книгу или пишет её содержимое в
базу, обязано лежать здесь и нигде больше:

* ``sanitize_reference.py`` — обезличивание книги перед коммитом;
* ``load_reference_fragment.py`` — заливка фрагмента в тестовое заведение, ``--purge``
  и замеры времени ответа.

Ни один модуль ``src/`` не имеет права импортировать этот пакет. Это не договорённость,
а проверяемое правило: ``tests/test_no_seed_data.py::test_src_does_not_import_tests``
разбирает AST каждого файла поставки и ловит даже упоминание строки ``tests.tools``.
Дополнительно ``.dockerignore`` исключает ``tests`` целиком, поэтому в образе этого
каталога нет вовсе.
"""
